# Core orchestration
langgraph>=0.2.34
langchain-core>=0.3.15

# Local, open-source LLM backend (Ollama - https://ollama.com, MIT licensed client)
# No API key required. If Ollama isn't running, the system falls back to a
# deterministic mock LLM so the prototype still runs end to end.
langchain-ollama>=0.2.0

# Config / storage / CLI
python-dotenv>=1.0.1
rich>=13.7.1
pydantic>=2.9.0

# Testing
pytest>=8.3.0
